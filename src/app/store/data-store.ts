import { BehaviorSubject } from 'rxjs/BehaviorSubject';
import { ProductDetail } from 'app/models/product-detail.model';

/**
 * This is a class that would contain BehaviorSubjects that can be subscribed to to fetch the current value
 * of the properties they represent. This is no longer relevant but leaving to explain later if desired.
 */
class DataStore {
    
}